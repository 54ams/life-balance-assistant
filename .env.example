# Expo client-side env (safe to expose — these are bundled into the app)
EXPO_PUBLIC_WHOOP_CLIENT_ID=

# Backend URL — leave blank for local dev (auto-detected from Expo)
# For production: set to your Render backend, e.g. https://life-balance-backend.onrender.com
EXPO_PUBLIC_BACKEND_URL=
